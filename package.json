{
  "name": "validacpf",
  "version": "1.0.0",
  "description": "projeto para lambda para validar CPF na base de dados",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Flavio",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.9.0"
  }
}
